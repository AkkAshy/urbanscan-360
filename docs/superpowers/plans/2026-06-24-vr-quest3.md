# VR-режим Quest 3 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Дать пользователю надеть Meta Quest 3, открыть 360°-объект в браузере шлема и в иммерсивном VR ходить по хотспотам, а также создавать/удалять связи контроллерами.

**Architecture:** Минимальный VR-слой поверх текущего A-Frame вьювера (подход A). Ввод контроллеров через `laser-controls` + raycaster по `.clickable`; навигация переиспользует `LinkArrows`; управление — новые in-scene компоненты `VRMenu` (HUD) и `VRPhotoPicker`, т.к. HTML-оверлеи в immersive не видны. Десктопный путь (мышь + HTML) не меняется. Новые VR-компоненты следуют существующему императивному паттерну (`useEffect` + `sceneRef` + `createElement` + cleanup, как `LinkArrows`/`LinkEditor`) — единообразие с кодбазой, без введения aframe-react/registerComponent.

**Tech Stack:** React 19 + Vite 7 + TypeScript 5.9 + A-Frame 1.7, zustand 5, pnpm. Тесты: vitest (новый, для чистой логики). VR-проверка: Immersive Web Emulator (Chrome) + реальный Quest 3 через Vercel-деплой.

**Дизайн-спек:** `docs/superpowers/specs/2026-06-24-vr-quest3-design.md`

---

## File Structure

**Создаём:**
- `frontend/src/utils/sphere.ts` — чистая геометрия: `skyPointToYawPitch`, `yawPitchToXyz`
- `frontend/src/utils/sphere.test.ts` — vitest
- `frontend/src/store/viewerStore.test.ts` — vitest для новых VR-полей
- `frontend/src/components/viewer/vr/VRMenu.tsx` — in-scene HUD-меню
- `frontend/src/components/viewer/vr/VRPhotoPicker.tsx` — in-scene выбор целевого фото
- `frontend/src/components/viewer/vr/VRLinkPlacer.tsx` — слушает клик по `<a-sky>` лучом → yaw/pitch
- `frontend/vitest.config.ts` — конфиг тестов

**Меняем:**
- `frontend/package.json` — devDep vitest + script `test`
- `frontend/src/store/viewerStore.ts` — `vrActive`, `vrPlacing` + экшены
- `frontend/src/components/viewer/AFrameScene.tsx` — руки-контроллеры, `enter-vr`/`exit-vr` → store
- `frontend/src/components/viewer/LinkArrows.tsx` — использовать `yawPitchToXyz` (DRY)
- `frontend/src/components/viewer/LinkEditor.tsx` — использовать `skyPointToYawPitch` (DRY)
- `frontend/src/pages/UploadPage.tsx` — рендер VR-UI при `vrActive`, HTML при `!vrActive`

---

## Phase 0 — Setup

### Task 1: Подключить vitest

**Files:**
- Modify: `frontend/package.json`
- Create: `frontend/vitest.config.ts`

- [ ] **Step 1: Установить vitest**

Run: `cd frontend && pnpm add -D vitest`
Expected: vitest появился в devDependencies.

- [ ] **Step 2: Добавить script в package.json**

В `frontend/package.json` в `"scripts"` добавить:

```json
    "test": "vitest run",
    "test:watch": "vitest"
```

- [ ] **Step 3: Создать vitest.config.ts**

`frontend/vitest.config.ts`:

```ts
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    environment: "node",
    include: ["src/**/*.test.ts"],
  },
});
```

- [ ] **Step 4: Проверить, что раннер стартует**

Run: `cd frontend && pnpm test`
Expected: vitest запускается, "No test files found" (тестов ещё нет) — это ОК, раннер работает.

- [ ] **Step 5: Commit**

```bash
git add frontend/package.json frontend/vitest.config.ts frontend/pnpm-lock.yaml
git commit -m "chore: add vitest for unit tests"
```

---

## Phase 1 — Контроллеры + навигация лучом

### Task 2: Утилита геометрии сферы (TDD)

Выносим формулы «точка на сфере ↔ yaw/pitch» из `LinkEditor`/`LinkArrows` в один модуль.

**Files:**
- Create: `frontend/src/utils/sphere.ts`
- Test: `frontend/src/utils/sphere.test.ts`

- [ ] **Step 1: Написать падающий тест**

`frontend/src/utils/sphere.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { skyPointToYawPitch, yawPitchToXyz } from "./sphere";

describe("skyPointToYawPitch", () => {
  it("точка спереди (0,0,-R) → yaw 0, pitch 0", () => {
    const r = skyPointToYawPitch({ x: 0, y: 0, z: -500 });
    expect(r.yaw).toBeCloseTo(0, 1);
    expect(r.pitch).toBeCloseTo(0, 1);
  });

  it("точка справа (R,0,0) → yaw 90", () => {
    const r = skyPointToYawPitch({ x: 500, y: 0, z: 0 });
    expect(r.yaw).toBeCloseTo(90, 1);
  });

  it("точка сверху → pitch ~90", () => {
    const r = skyPointToYawPitch({ x: 0, y: 500, z: 0 });
    expect(r.pitch).toBeCloseTo(90, 1);
  });

  it("yaw всегда в диапазоне 0..360", () => {
    const r = skyPointToYawPitch({ x: -1, y: 0, z: 1 }); // сзади-слева
    expect(r.yaw).toBeGreaterThanOrEqual(0);
    expect(r.yaw).toBeLessThan(360);
  });
});

describe("round-trip yawPitchToXyz → skyPointToYawPitch", () => {
  it("сохраняет yaw/pitch", () => {
    for (const [yaw, pitch] of [[0, 0], [90, 0], [200, 15], [340, -30]]) {
      const p = yawPitchToXyz(yaw, pitch, 8);
      const back = skyPointToYawPitch(p);
      expect(back.yaw).toBeCloseTo(yaw, 1);
      expect(back.pitch).toBeCloseTo(pitch, 1);
    }
  });
});
```

- [ ] **Step 2: Запустить, убедиться что падает**

Run: `cd frontend && pnpm test`
Expected: FAIL — `skyPointToYawPitch is not a function` / модуль не найден.

- [ ] **Step 3: Реализовать утилиту**

`frontend/src/utils/sphere.ts`:

```ts
/** Точка в 3D на сфере (координаты A-Frame). */
export interface Vec3 {
  x: number;
  y: number;
  z: number;
}

/** Угловое положение на сфере. yaw 0..360 (горизонталь), pitch -90..90 (вертикаль). */
export interface YawPitch {
  yaw: number;
  pitch: number;
}

/**
 * Точка пересечения луча со сферой → yaw/pitch.
 * yaw отсчитывается от -Z (перёд) по часовой; нормализуется в 0..360.
 */
export function skyPointToYawPitch({ x, y, z }: Vec3): YawPitch {
  let yaw = Math.atan2(x, -z) * (180 / Math.PI);
  if (yaw < 0) yaw += 360;
  const pitch = Math.atan2(y, Math.sqrt(x * x + z * z)) * (180 / Math.PI);
  return { yaw, pitch };
}

/** yaw/pitch + радиус → точка в 3D (обратное к skyPointToYawPitch). */
export function yawPitchToXyz(yaw: number, pitch: number, radius: number): Vec3 {
  const yawRad = (yaw * Math.PI) / 180;
  const pitchRad = (pitch * Math.PI) / 180;
  const hR = Math.cos(pitchRad) * radius;
  return {
    x: Math.sin(yawRad) * hR,
    y: Math.sin(pitchRad) * radius,
    z: -Math.cos(yawRad) * hR,
  };
}
```

- [ ] **Step 4: Запустить, убедиться что проходит**

Run: `cd frontend && pnpm test`
Expected: PASS — все тесты sphere зелёные.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/utils/sphere.ts frontend/src/utils/sphere.test.ts
git commit -m "feat: add sphere geometry util (yaw/pitch <-> xyz)"
```

---

### Task 3: Применить util в LinkArrows и LinkEditor (DRY-рефактор)

Заменяем дублирующиеся формулы на `sphere.ts`. Поведение не меняется — десктоп-вьювер работает как прежде.

**Files:**
- Modify: `frontend/src/components/viewer/LinkArrows.tsx:53-60`
- Modify: `frontend/src/components/viewer/LinkEditor.tsx:56-62`

- [ ] **Step 1: LinkArrows — использовать `yawPitchToXyz`**

В `LinkArrows.tsx` добавить импорт вверху:

```ts
import { yawPitchToXyz } from "../../utils/sphere";
```

Заменить блок вычисления координат (строки ~54-60):

```ts
      const yawRad = (link.yaw * Math.PI) / 180;
      const pitchRad = (link.pitch * Math.PI) / 180;
      const hR = Math.cos(pitchRad) * ARROW_RADIUS;

      const x = Math.sin(yawRad) * hR;
      const y = Math.sin(pitchRad) * ARROW_RADIUS;
      const z = -Math.cos(yawRad) * hR;
```

на:

```ts
      const { x, y, z } = yawPitchToXyz(link.yaw, link.pitch, ARROW_RADIUS);
```

- [ ] **Step 2: LinkEditor — использовать `skyPointToYawPitch`**

В `LinkEditor.tsx` добавить импорт:

```ts
import { skyPointToYawPitch } from "../../utils/sphere";
```

Заменить блок в `handleClick` (строки ~56-63):

```ts
      const { x, y, z } = detail.intersection.point;
      // Конвертация xyz на сфере → yaw/pitch
      let yaw = Math.atan2(x, -z) * (180 / Math.PI);
      if (yaw < 0) yaw += 360;
      const pitch =
        Math.atan2(y, Math.sqrt(x * x + z * z)) * (180 / Math.PI);

      setPlacing({ yaw, pitch });
```

на:

```ts
      const { yaw, pitch } = skyPointToYawPitch(detail.intersection.point);
      setPlacing({ yaw, pitch });
```

- [ ] **Step 3: Проверить сборку типов**

Run: `cd frontend && pnpm build`
Expected: `tsc -b` без ошибок, vite build успешен.

- [ ] **Step 4: Ручная проверка десктоп-регрессии**

Run: `cd frontend && pnpm dev` (нужен локальный бэк на :8000 или прод).
Проверить в браузере: открыть папку → фото → 360° → стрелки на местах; режим «Связи» → клик по сцене ставит стрелку. Поведение как раньше.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/viewer/LinkArrows.tsx frontend/src/components/viewer/LinkEditor.tsx
git commit -m "refactor: reuse sphere util in LinkArrows/LinkEditor (DRY)"
```

---

### Task 4: Добавить VR-состояние в store (TDD)

**Files:**
- Modify: `frontend/src/store/viewerStore.ts`
- Test: `frontend/src/store/viewerStore.test.ts`

- [ ] **Step 1: Написать падающий тест**

`frontend/src/store/viewerStore.test.ts`:

```ts
import { describe, it, expect, beforeEach } from "vitest";
import { useViewerStore } from "./viewerStore";

describe("viewerStore VR-состояние", () => {
  beforeEach(() => {
    useViewerStore.setState({ vrActive: false, vrPlacing: null });
  });

  it("дефолты: vrActive=false, vrPlacing=null", () => {
    const s = useViewerStore.getState();
    expect(s.vrActive).toBe(false);
    expect(s.vrPlacing).toBeNull();
  });

  it("setVrActive переключает флаг", () => {
    useViewerStore.getState().setVrActive(true);
    expect(useViewerStore.getState().vrActive).toBe(true);
  });

  it("setVrPlacing сохраняет и сбрасывает yaw/pitch", () => {
    useViewerStore.getState().setVrPlacing({ yaw: 90, pitch: 10 });
    expect(useViewerStore.getState().vrPlacing).toEqual({ yaw: 90, pitch: 10 });
    useViewerStore.getState().setVrPlacing(null);
    expect(useViewerStore.getState().vrPlacing).toBeNull();
  });
});
```

- [ ] **Step 2: Запустить, убедиться что падает**

Run: `cd frontend && pnpm test`
Expected: FAIL — `setVrActive is not a function`.

- [ ] **Step 3: Добавить поля и экшены в стор**

В `viewerStore.ts` в интерфейс `ViewerState` добавить (после `linkEditMode`):

```ts
  /** В иммерсивном VR ли сейчас (из enter-vr/exit-vr) */
  vrActive: boolean;
  /** Ожидается выбор цели связи: точка, куда поставили стрелку */
  vrPlacing: { yaw: number; pitch: number } | null;
  setVrActive: (on: boolean) => void;
  setVrPlacing: (p: { yaw: number; pitch: number } | null) => void;
```

В `create<ViewerState>(...)` добавить начальные значения (рядом с `linkEditMode: false`):

```ts
  vrActive: false,
  vrPlacing: null,
```

И экшены (рядом с `setLinkEditMode`):

```ts
  setVrActive: (on) => set({ vrActive: on }),
  setVrPlacing: (p) => set({ vrPlacing: p }),
```

- [ ] **Step 4: Запустить, убедиться что проходит**

Run: `cd frontend && pnpm test`
Expected: PASS — тесты store зелёные (и sphere тоже).

- [ ] **Step 5: Commit**

```bash
git add frontend/src/store/viewerStore.ts frontend/src/store/viewerStore.test.ts
git commit -m "feat: add vrActive/vrPlacing to viewer store"
```

---

### Task 5: Контроллеры Quest + enter-vr/exit-vr в AFrameScene

Добавляем две руки с лучом и raycaster по `.clickable`. На вход/выход VR — обновляем `vrActive`.

**Files:**
- Modify: `frontend/src/components/viewer/AFrameScene.tsx`

- [ ] **Step 1: Импортировать store**

В `AFrameScene.tsx` добавить импорт:

```ts
import { useViewerStore } from "../../store/viewerStore";
```

- [ ] **Step 2: Добавить руки-контроллеры при создании сцены**

В `AFrameScene.tsx`, внутри первого `useEffect` (создание сцены), ПОСЛЕ `scene.appendChild(camera);` и ДО `containerRef.current.appendChild(scene);` вставить:

```ts
    // VR-контроллеры (две руки): луч + raycaster по .clickable.
    // На десктопе не мешают (видны только в immersive).
    for (const hand of ["left", "right"]) {
      const controller = document.createElement("a-entity");
      controller.setAttribute("laser-controls", `hand: ${hand}`);
      controller.setAttribute(
        "raycaster",
        "objects: .clickable; far: 100; lineColor: #3b82f6; lineOpacity: 0.85"
      );
      controller.setAttribute("data-vr-hand", hand);
      scene.appendChild(controller);
    }
```

- [ ] **Step 3: Слушать enter-vr/exit-vr → store**

В `AFrameScene.tsx`, внутри того же `useEffect`, ПОСЛЕ блока контроллеров добавить:

```ts
    const setVrActive = useViewerStore.getState().setVrActive;
    const onEnterVR = () => setVrActive(true);
    const onExitVR = () => setVrActive(false);
    scene.addEventListener("enter-vr", onEnterVR);
    scene.addEventListener("exit-vr", onExitVR);
```

И в функции очистки (`return () => {...}`) ДО удаления сцены добавить:

```ts
      scene.removeEventListener("enter-vr", onEnterVR);
      scene.removeEventListener("exit-vr", onExitVR);
      setVrActive(false);
```

- [ ] **Step 4: Проверить сборку**

Run: `cd frontend && pnpm build`
Expected: без ошибок типов.

- [ ] **Step 5: Проверить навигацию лучом в эмуляторе**

Установить Chrome-расширение «Immersive Web Emulator». Run: `pnpm dev`, открыть вьювер с фото, где есть хотспоты → DevTools → вкладка эмулятора → «Enter VR». Навести луч правого контроллера на стрелку → нажать триггер (кнопка Trigger в эмуляторе) → должен произойти переход на связанное фото.
Expected: переход по стрелке работает лучом; счётчик HTML пока виден (уберём в Фазе 2).

- [ ] **Step 6: Commit**

```bash
git add frontend/src/components/viewer/AFrameScene.tsx
git commit -m "feat: add Quest controllers + enter/exit-vr tracking to scene"
```

> **Веха Фазы 1:** можно войти в VR и ходить по существующим хотспотам лучом.

---

## Phase 2 — VRMenu (HUD)

### Task 6: Компонент VRMenu (in-scene HUD)

Меню крепится к камере (HUD), всегда перед глазами снизу. Кнопки: `‹` `›`, счётчик, toggle «Связи», «Создать», «Выход».

**Files:**
- Create: `frontend/src/components/viewer/vr/VRMenu.tsx`

- [ ] **Step 1: Создать VRMenu.tsx**

`frontend/src/components/viewer/vr/VRMenu.tsx`:

```tsx
import { useEffect, useRef } from "react";
import { useViewerStore } from "../../../store/viewerStore";

interface Props {
  sceneRef: React.MutableRefObject<HTMLElement | null>;
}

/** Делает плоскую кнопку с подписью; возвращает кликабельный a-entity. */
function makeButton(label: string, x: number, width: number, color: string) {
  const btn = document.createElement("a-entity");
  btn.setAttribute("position", `${x} 0 0`);
  btn.classList.add("clickable");

  const bg = document.createElement("a-plane");
  bg.setAttribute("width", String(width));
  bg.setAttribute("height", "0.18");
  bg.setAttribute("color", color);
  bg.setAttribute("material", "opacity: 0.85; shader: flat");
  bg.classList.add("clickable");

  const text = document.createElement("a-text");
  text.setAttribute("value", label);
  text.setAttribute("align", "center");
  text.setAttribute("color", "#FFFFFF");
  text.setAttribute("width", "2.5");
  text.setAttribute("position", "0 0 0.01");

  btn.appendChild(bg);
  btn.appendChild(text);
  return btn;
}

/**
 * In-scene HUD-меню для VR. Крепится к камере сцены (всегда перед глазами).
 * Рендерится только когда vrActive (управляется из UploadPage).
 */
export function VRMenu({ sceneRef }: Props) {
  const menuRef = useRef<HTMLElement | null>(null);
  const {
    currentIndex,
    photos,
    next,
    prev,
    linkEditMode,
    setLinkEditMode,
    setVrPlacing,
    setVrActive,
  } = useViewerStore();

  // Создаём контейнер меню один раз и крепим к камере
  useEffect(() => {
    const scene = sceneRef.current;
    if (!scene) return;
    const camera = scene.querySelector("a-camera") || scene.querySelector("[camera]");
    if (!camera) return;

    const menu = document.createElement("a-entity");
    // HUD: ниже линии взгляда, на расстоянии 1.6 м
    menu.setAttribute("position", "0 -0.7 -1.6");
    camera.appendChild(menu);
    menuRef.current = menu;

    return () => {
      menu.parentNode?.removeChild(menu);
      menuRef.current = null;
    };
  }, [sceneRef]);

  // Перерисовываем кнопки при смене состояния
  useEffect(() => {
    const menu = menuRef.current;
    if (!menu) return;
    while (menu.firstChild) menu.removeChild(menu.firstChild);

    const counter = document.createElement("a-text");
    counter.setAttribute("value", `${currentIndex + 1} / ${photos.length}`);
    counter.setAttribute("align", "center");
    counter.setAttribute("color", "#FFFFFF");
    counter.setAttribute("width", "2.5");
    counter.setAttribute("position", "0 0.18 0");
    menu.appendChild(counter);

    const prevBtn = makeButton("‹", -0.6, 0.2, "#1f2937");
    prevBtn.addEventListener("click", () => prev());
    menu.appendChild(prevBtn);

    const nextBtn = makeButton("›", -0.35, 0.2, "#1f2937");
    nextBtn.addEventListener("click", () => next());
    menu.appendChild(nextBtn);

    const linkBtn = makeButton(
      linkEditMode ? "Связи: вкл" : "Связи",
      0.05,
      0.4,
      linkEditMode ? "#ea580c" : "#1f2937"
    );
    linkBtn.addEventListener("click", () => setLinkEditMode(!linkEditMode));
    menu.appendChild(linkBtn);

    const createBtn = makeButton("Создать", 0.45, 0.4, "#2563eb");
    createBtn.addEventListener("click", () => {
      setLinkEditMode(true);
      setVrPlacing(null); // ждём клик по сцене (см. VRLinkPlacer)
    });
    menu.appendChild(createBtn);

    const exitBtn = makeButton("Выход", 0.85, 0.4, "#7f1d1d");
    exitBtn.addEventListener("click", () => {
      const scene = sceneRef.current as unknown as { exitVR?: () => void };
      scene?.exitVR?.();
      setVrActive(false);
    });
    menu.appendChild(exitBtn);
  }, [
    currentIndex,
    photos.length,
    linkEditMode,
    next,
    prev,
    setLinkEditMode,
    setVrPlacing,
    setVrActive,
    sceneRef,
  ]);

  return null;
}
```

- [ ] **Step 2: Рендерить VRMenu при vrActive в UploadPage**

В `UploadPage.tsx` добавить импорт:

```ts
import { VRMenu } from "../components/viewer/vr/VRMenu";
```

Достать `vrActive` из стора (в деструктуризации `useViewerStore()`):

```ts
    vrActive,
```

Внутри блока вьювера (`{viewerOpen && currentViewerPhoto && (...)}`), сразу после `<AFrameScene .../>` добавить:

```tsx
          {vrActive && <VRMenu sceneRef={sceneRef} />}
```

- [ ] **Step 3: Проверить сборку**

Run: `cd frontend && pnpm build`
Expected: без ошибок.

- [ ] **Step 4: Проверка в эмуляторе**

`pnpm dev` → вьювер → Enter VR (эмулятор). Снизу перед глазами — меню с счётчиком и кнопками. Навести луч → `‹`/`›` листают фото; «Выход» выходит из VR.
Expected: меню видно и кликается лучом.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/viewer/vr/VRMenu.tsx frontend/src/pages/UploadPage.tsx
git commit -m "feat: in-scene VR HUD menu (nav/counter/exit/link toggle)"
```

---

### Task 7: Прятать HTML-оверлеи в VR

В immersive HTML невидим, но на десктопе он есть. Прячем десктоп-оверлеи когда `vrActive`, чтобы при выходе из VR не было дубля и чтобы логика была чистой.

**Files:**
- Modify: `frontend/src/pages/UploadPage.tsx`

- [ ] **Step 1: Обернуть HTML-контролы условием `!vrActive`**

В `UploadPage.tsx` в блоке вьювера обернуть HTML-группы в `{!vrActive && (...)}`:
- блок кнопок «закрыть + связи» (`<div className="absolute top-4 right-4 ...">`)
- `<LinkEditor ... />` (десктопный редактор)
- `<NavigationArrows />`
- `<ThumbnailStrip />`
- счётчик (`<div className="absolute top-4 left-1/2 ...">`)

Пример для счётчика:

```tsx
          {!vrActive && (
            <div className="absolute top-4 left-1/2 -translate-x-1/2 z-20 px-3 py-1 rounded-full bg-black/50 text-white text-sm backdrop-blur-sm">
              {currentIndex + 1} / {viewerPhotos.length}
            </div>
          )}
```

`<LinkArrows />` НЕ оборачивать — стрелки нужны и в VR, и на десктопе.

- [ ] **Step 2: Проверить сборку**

Run: `cd frontend && pnpm build`
Expected: без ошибок.

- [ ] **Step 3: Ручная проверка обоих режимов**

`pnpm dev`. Десктоп: всё на месте (кнопки, лента, счётчик). Enter VR (эмулятор): HTML-оверлеи скрыты, виден только in-scene VRMenu + стрелки. Exit VR: HTML вернулся.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/pages/UploadPage.tsx
git commit -m "feat: hide HTML overlays while in VR"
```

> **Веха Фазы 2:** в VR есть рабочее меню; десктоп без регрессий.

---

## Phase 3 — VR-редактирование связей

### Task 8: VRLinkPlacer — клик по сцене лучом → yaw/pitch

Когда нажата «Создать», следующий клик лучом по `<a-sky>` фиксирует точку (yaw/pitch) в `vrPlacing`.

**Files:**
- Create: `frontend/src/components/viewer/vr/VRLinkPlacer.tsx`

- [ ] **Step 1: Создать VRLinkPlacer.tsx**

`frontend/src/components/viewer/vr/VRLinkPlacer.tsx`:

```tsx
import { useEffect } from "react";
import { useViewerStore } from "../../../store/viewerStore";
import { skyPointToYawPitch } from "../../../utils/sphere";

interface Props {
  sceneRef: React.MutableRefObject<HTMLElement | null>;
  /** true когда ждём клик по сцене для постановки стрелки */
  arming: boolean;
}

/**
 * В VR-режиме «Создать»: ловит клик лучом по <a-sky>, считает yaw/pitch,
 * кладёт в vrPlacing (после чего показывается VRPhotoPicker).
 */
export function VRLinkPlacer({ sceneRef, arming }: Props) {
  const setVrPlacing = useViewerStore((s) => s.setVrPlacing);

  useEffect(() => {
    if (!arming) return;
    const scene = sceneRef.current;
    if (!scene) return;
    const sky = scene.querySelector("#photo-sky");
    if (!sky) return;

    sky.classList.add("clickable");
    const onClick = (e: Event) => {
      const detail = (e as CustomEvent).detail;
      if (!detail?.intersection?.point) return;
      setVrPlacing(skyPointToYawPitch(detail.intersection.point));
    };
    sky.addEventListener("click", onClick);

    return () => {
      sky.removeEventListener("click", onClick);
      // класс clickable у sky оставляем — он нужен и десктопному LinkEditor
    };
  }, [arming, sceneRef, setVrPlacing]);

  return null;
}
```

- [ ] **Step 2: Подключить в UploadPage**

В `UploadPage.tsx` импорт:

```ts
import { VRLinkPlacer } from "../components/viewer/vr/VRLinkPlacer";
```

Достать `vrPlacing` и `linkEditMode` из стора (если ещё не). В блоке вьювера после `<VRMenu .../>`:

```tsx
          {vrActive && (
            <VRLinkPlacer sceneRef={sceneRef} arming={linkEditMode && vrPlacing === null} />
          )}
```

- [ ] **Step 3: Проверить сборку**

Run: `cd frontend && pnpm build`
Expected: без ошибок.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/components/viewer/vr/VRLinkPlacer.tsx frontend/src/pages/UploadPage.tsx
git commit -m "feat: VR ray-click on sky -> yaw/pitch (vrPlacing)"
```

---

### Task 9: VRPhotoPicker — выбор цели и создание связи

Когда `vrPlacing != null`, показываем дугу превью доступных фото. Клик → создаём связь.

**Files:**
- Create: `frontend/src/components/viewer/vr/VRPhotoPicker.tsx`
- Modify: `frontend/src/pages/UploadPage.tsx`

- [ ] **Step 1: Создать VRPhotoPicker.tsx**

`frontend/src/components/viewer/vr/VRPhotoPicker.tsx`:

```tsx
import { useEffect, useRef } from "react";
import { mediaUrl } from "../../../api/client";
import { createPhotoLink } from "../../../api/photos";
import { useViewerStore } from "../../../store/viewerStore";
import { yawPitchToXyz } from "../../../utils/sphere";
import type { PhotoViewer, PhotoLink } from "../../../types";

interface Props {
  sceneRef: React.MutableRefObject<HTMLElement | null>;
  photos: PhotoViewer[];
  currentPhoto: PhotoViewer;
  links: PhotoLink[];
  onLinksChanged: () => void;
}

/**
 * In-scene выбор целевого фото при создании связи в VR.
 * Превью располагаются дугой перед пользователем; клик создаёт PhotoLink.
 */
export function VRPhotoPicker({
  sceneRef,
  photos,
  currentPhoto,
  links,
  onLinksChanged,
}: Props) {
  const containerRef = useRef<HTMLElement | null>(null);
  const { vrPlacing, setVrPlacing } = useViewerStore();

  useEffect(() => {
    const scene = sceneRef.current;
    if (!scene || !vrPlacing) return;

    const linkedIds = new Set(links.map((l) => l.to_photo));
    const available = photos.filter(
      (p) => p.id !== currentPhoto.id && !linkedIds.has(p.id)
    );

    const container = document.createElement("a-entity");
    scene.appendChild(container);
    containerRef.current = container;

    if (available.length === 0) {
      const empty = document.createElement("a-text");
      empty.setAttribute("value", "Все фото уже привязаны");
      empty.setAttribute("align", "center");
      empty.setAttribute("color", "#FFFFFF");
      empty.setAttribute("width", "4");
      empty.setAttribute("position", "0 1.6 -3");
      container.appendChild(empty);
    }

    // Дуга превью на уровне глаз, радиус 3 м, шаг 18°
    available.forEach((photo, i) => {
      const spread = 18;
      const yaw = (i - (available.length - 1) / 2) * spread;
      const { x, y, z } = yawPitchToXyz(yaw < 0 ? yaw + 360 : yaw, 0, 3);

      const tile = document.createElement("a-image");
      tile.setAttribute("src", mediaUrl(photo.thumbnail || photo.image));
      tile.setAttribute("width", "0.8");
      tile.setAttribute("height", "0.5");
      tile.setAttribute("position", `${x} ${1.6 + y} ${z}`);
      tile.setAttribute("look-at", "[camera]");
      tile.setAttribute("material", "shader: flat; side: double");
      tile.classList.add("clickable");

      const caption = document.createElement("a-text");
      caption.setAttribute("value", photo.title || `#${photo.id}`);
      caption.setAttribute("align", "center");
      caption.setAttribute("color", "#FFFFFF");
      caption.setAttribute("width", "2");
      caption.setAttribute("position", "0 -0.35 0");
      tile.appendChild(caption);

      tile.addEventListener("click", async () => {
        if (!vrPlacing) return;
        try {
          await createPhotoLink(currentPhoto.id, photo.id, vrPlacing.yaw, vrPlacing.pitch);
          setVrPlacing(null);
          onLinksChanged();
        } catch (err) {
          console.error("VR: ошибка создания связи:", err);
        }
      });

      container.appendChild(tile);
    });

    return () => {
      container.parentNode?.removeChild(container);
      containerRef.current = null;
    };
  }, [sceneRef, vrPlacing, photos, currentPhoto, links, onLinksChanged, setVrPlacing]);

  return null;
}
```

- [ ] **Step 2: Подключить в UploadPage**

Импорт:

```ts
import { VRPhotoPicker } from "../components/viewer/vr/VRPhotoPicker";
```

После `<VRLinkPlacer .../>`:

```tsx
          {vrActive && vrPlacing && (
            <VRPhotoPicker
              sceneRef={sceneRef}
              photos={viewerPhotos}
              currentPhoto={currentViewerPhoto}
              links={links}
              onLinksChanged={fetchLinks}
            />
          )}
```

- [ ] **Step 3: Проверить сборку**

Run: `cd frontend && pnpm build`
Expected: без ошибок.

- [ ] **Step 4: Проверка цикла создания в эмуляторе**

`pnpm dev` → вьювер с ≥2 фото → Enter VR → «Создать» → клик лучом по сцене → появляется дуга превью → клик по превью → стрелка-хотспот появляется, дуга исчезает.
Expected: связь создаётся, появляется новая стрелка (через fetchLinks).

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/viewer/vr/VRPhotoPicker.tsx frontend/src/pages/UploadPage.tsx
git commit -m "feat: VR target picker creates photo links"
```

---

### Task 10: Удаление связей в VR

`LinkArrows` уже умеет удалять в `editMode` (клик по стрелке → `onDeleteLink`). В VR `editMode = linkEditMode` (toggle в VRMenu). Проверяем, что путь работает лучом, без нового кода.

**Files:**
- (проверка; код не меняется — `LinkArrows` уже получает `editMode={linkEditMode}` в UploadPage)

- [ ] **Step 1: Проверка удаления в эмуляторе**

`pnpm dev` → вьювер с хотспотами → Enter VR → в VRMenu нажать «Связи» (включить editMode) → стрелки станут оранжевыми → навести луч на стрелку → триггер → связь удаляется (`deletePhotoLink` + `fetchLinks`).
Expected: стрелка исчезает, режим «Связи: вкл» подсвечен.

- [ ] **Step 2: Если стрелки не реагируют — увеличить хитбокс (только при необходимости)**

Если лучом сложно попасть по стрелке: в `LinkArrows.tsx` у `img` добавить невидимую кликабельную подложку. В `links.forEach`, после создания `img`, перед `arrow.appendChild(img)`:

```ts
      const hit = document.createElement("a-plane");
      hit.setAttribute("width", "1.6");
      hit.setAttribute("height", "1.9");
      hit.setAttribute("material", "opacity: 0; transparent: true; shader: flat");
      hit.classList.add("clickable");
      hit.addEventListener("click", () => {
        if (editMode && onDeleteLink) onDeleteLink(link.id);
        else onNavigate(link.to_photo);
      });
      arrow.appendChild(hit);
```

- [ ] **Step 3: Commit (если был Step 2)**

```bash
git add frontend/src/components/viewer/LinkArrows.tsx
git commit -m "feat: larger hit area for VR arrow targeting"
```

> **Веха Фазы 3:** полный цикл просмотр + создание + удаление связей в VR.

---

## Phase 4 — Полировка

### Task 11: Фейд при переходе между фото

Мягкий blackout на смене фото убирает резкий «телепорт».

**Files:**
- Modify: `frontend/src/components/viewer/AFrameScene.tsx`

- [ ] **Step 1: Добавить фейд-сферу вокруг камеры**

В `AFrameScene.tsx`, во втором `useEffect` (обновление `src` при смене `photoUrl`), обернуть смену в фейд. Заменить тело:

```ts
  useEffect(() => {
    const sky = containerRef.current?.querySelector("#photo-sky");
    if (sky && photoUrl) {
      sky.setAttribute("src", photoUrl);
    }
  }, [photoUrl]);
```

на:

```ts
  useEffect(() => {
    const container = containerRef.current;
    const sky = container?.querySelector("#photo-sky");
    const camera = container?.querySelector("a-camera");
    if (!sky || !photoUrl) return;

    // Фейд через чёрную сферу у камеры
    let fade = camera?.querySelector("#fade-sphere") as HTMLElement | null;
    if (camera && !fade) {
      fade = document.createElement("a-sphere");
      fade.setAttribute("id", "fade-sphere");
      fade.setAttribute("radius", "0.4");
      fade.setAttribute("material", "color: #000; shader: flat; side: back; opacity: 0; transparent: true");
      camera.appendChild(fade);
    }
    fade?.setAttribute("animation__out", "property: material.opacity; to: 1; dur: 120");
    const t = setTimeout(() => {
      sky.setAttribute("src", photoUrl);
      fade?.setAttribute("animation__in", "property: material.opacity; from: 1; to: 0; dur: 200");
    }, 130);

    return () => clearTimeout(t);
  }, [photoUrl]);
```

- [ ] **Step 2: Проверить сборку + эмулятор**

Run: `cd frontend && pnpm build` → без ошибок. `pnpm dev` → переходы по стрелкам с лёгким затемнением. Десктоп тоже плавнее.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/viewer/AFrameScene.tsx
git commit -m "feat: fade transition between photos"
```

---

### Task 12: Hover-подсветка интерактивных объектов

Луч наводится — объект подсвечивается (понятнее прицеливание).

**Files:**
- Modify: `frontend/src/components/viewer/LinkArrows.tsx`

- [ ] **Step 1: Подсветка стрелки на hover**

В `LinkArrows.tsx` в `links.forEach`, после `arrow.addEventListener("click", ...)` добавить:

```ts
      arrow.addEventListener("mouseenter", () => {
        img.setAttribute("scale", "1.2 1.2 1.2");
      });
      arrow.addEventListener("mouseleave", () => {
        img.setAttribute("scale", "1 1 1");
      });
```

> A-Frame raycaster (мышь и laser-controls) эмитит `mouseenter`/`mouseleave` на пересечённых `.clickable` — работает и для контроллеров.

- [ ] **Step 2: Проверить сборку + эмулятор**

Run: `cd frontend && pnpm build`. `pnpm dev` → наводя луч/курсор на стрелку — она увеличивается.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/viewer/LinkArrows.tsx
git commit -m "feat: hover highlight on hotspot arrows"
```

---

### Task 13: Финальный прогон на Quest 3 + деплой

**Files:** (без изменений кода; деплой)

- [ ] **Step 1: Полный прогон в эмуляторе по чек-листу**

Проверить: вход/выход VR · навигация лучом · переключение `‹ ›` · создание связи · удаление связи · десктоп-регрессия (мышь/HTML работают).

- [ ] **Step 2: Прогнать тесты и сборку**

Run: `cd frontend && pnpm test && pnpm build`
Expected: тесты зелёные, сборка успешна.

- [ ] **Step 3: Деплой на Vercel**

Run: `cd frontend && vercel --prod`
Expected: задеплоен, prod URL `https://urbanscan360.vercel.app`.

- [ ] **Step 4: Проверка на реальном Quest 3**

В браузере Quest 3 открыть `https://urbanscan360.vercel.app` → залогиниться → папка → фото → 360° → «Enter VR». Пройти весь чек-лист на железе.
Expected: VR-режим работает на Quest 3.

- [ ] **Step 5: Записать в канал коллег-Claude**

Создать файл-отчёт и скопировать на сервер:

```bash
scp /tmp/vr-quest3-done.md "vps_prod:/root/kanat/claude-comms/urbanscan-360/2026-06-24 — VR-режим Quest 3.md"
```

(содержимое: что сделано, изменённые файлы, задеплоено ли, открытые вопросы — по шаблону канала)

- [ ] **Step 6: Обновить Obsidian-vault**

Дополнить `~/Projects/Obsidian/UrbanScan/urbanscan-360/Фронтенд.md` секцией про VR-режим и снять задачу в `Задачи/Текущие задачи.md`.

---

## Self-Review

**Spec coverage** (сверка с дизайн-спеком):
- §5 архитектура (контроллеры + reuse LinkArrows + in-scene UI) → Tasks 5, 6, 9 ✅
- §6.1 AFrameScene правки → Task 5 ✅
- §6.2 VRControllers (руки) → Task 5 (внутри AFrameScene, осознанно) ✅
- §6.3 VRMenu (HUD) → Task 6 ✅
- §6.4 VRPhotoPicker → Task 9 ✅
- §6.5 skyPointToYawPitch util → Task 2 + reuse Task 3 ✅
- §6.6 LinkArrows reuse/hitbox/hover → Tasks 3, 10, 12 ✅
- §7 store vrActive/vrPlacing → Task 4 ✅
- §8 потоки (вход/навигация/создать/удалить/выход) → Tasks 5,6,8,9,10 ✅
- §9 края (нет фото для связи, exit-vr, десктоп) → Tasks 9 (empty), 5 (exit), 7 ✅
- §9 комфорт (фейд) → Task 11 ✅
- §10 тест (эмулятор + Quest + Vercel) → Tasks с эмулятором + Task 13 ✅

**Placeholder scan:** код приведён полностью в каждом шаге, плейсхолдеров нет.

**Type consistency:** `skyPointToYawPitch(Vec3)→YawPitch`, `yawPitchToXyz(yaw,pitch,radius)→Vec3`, `setVrActive(boolean)`, `setVrPlacing({yaw,pitch}|null)`, `createPhotoLink(fromId,toId,yaw,pitch)`, `deletePhotoLink(linkId)` — согласованы с `api/photos.ts` и `types/index.ts`.

**Отступления от спека (осознанные):** VRControllers реализован как руки внутри AFrameScene (а не отдельный React-компонент) — руки постоянная часть rig, так надёжнее; VR-компоненты в императивном React-стиле (как LinkArrows), без registerComponent — единообразие с кодбазой.
